# pyproject.toml is the source of truth for dependencies. This file is kept
# for the plain `pip install -r requirements.txt` workflow; for development
# prefer `pip install -e ".[dev]"`.
anthropic>=0.40.0
python-dotenv>=1.0.0
requests>=2.31.0
pytest>=8.0.0
pytest-cov>=5.0.0
biopython>=1.83

# pLannotate is NOT available on PyPI. Install via conda using environment.yml:
#   conda env create -f environment.yml
#   conda activate claude-plasmids
